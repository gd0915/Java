package day15multidimensionalarraylists;

public class List01 {

    public static void main(String[] args) {

        //When you create an Array you have to declare the length of the Array. Otherwise, it is impossible to create the Array
        //When you use "List" no need to declare the number of elements at the beginning
        //"Lists" are flexible for length, but Arrays are not

        //Lists are for storing multiple data in the same data types
        //Lists can store non-primitive data types, Arrays can not
        //List cannot store primitive data types.
        //Lists have many useful methods, Arrays do not

        //Arrays are so fast
        //Arrays uses memory so less

        int arr[] = new int[5];
    }
}
